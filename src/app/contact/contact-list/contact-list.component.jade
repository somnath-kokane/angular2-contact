.row
  .col-md-6
    p
      a.btn.btn-primary(routerLink='form/new') New Contact

    .list-group('*ngIf'='contacts')
      a.list-group-item('*ngFor'='let contact of contacts | async' 
        routerLink='detail/{{contact.id}}')
          span {{contact.name}}
